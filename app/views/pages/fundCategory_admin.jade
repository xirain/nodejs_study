extends ../layout

block content
   .container
     .row
        form.form-horizontal(method="post", action="/admin/fundCategory")
            .form-group
              label.col-sm-2.control-label(for="inputCategory") 基金分类
              .col-sm-10
                 input#inputTitile.form-control(type="text",name="fundCategory[name]",value=fundCategory.name)
                .col-sm-offset-2.col-sm-10
                  button.btn.btn-default(type="submit") 录入 
        
